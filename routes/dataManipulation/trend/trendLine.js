function trendLine(radicalPricesInfo) {
    const pricesByDate = []
    for(let price of radicalPricesInfo) {
        for(let i = 1; i < radicalPricesInfo.length; i++) {
            const incline = price.price
        } 
    }
}